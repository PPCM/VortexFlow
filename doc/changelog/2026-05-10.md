# Changelog - 2026-05-10

## Summary
Initialized ruflo (claude-flow) inside the project to wire up Claude Code
hooks. The `.claude/helpers/hook-handler.cjs` referenced by the global
`~/.claude/settings.json` was missing locally, which produced
`PreToolUse:Bash hook error / cjs/loader:1404 (MODULE_NOT_FOUND)` lines on
every tool call. `ruflo init` deposited the helpers; the hooks now run
cleanly. Also refreshed `CLAUDE.md` (off-repo) to match the recent ESLint /
bundle-split / CI changes that landed last commits.

## Changes

- [.gitignore] Added a Ruflo runtime block ignoring `.claude/`,
  `.claude-flow/`, `.swarm/`, and `.mcp.json`. These directories are all
  per-project state created by `ruflo init`, `ruflo memory init`,
  `ruflo swarm init`, and the daemon — none of it belongs in the repo.
- [CLAUDE.md] (off-repo, gitignored) Updated three sections so future
  Claude Code sessions don't operate on stale docs:
  - Frontend ESLint is now documented as wired up (flat config at
    `frontend/eslint.config.js`, `npm run lint` / `lint:fix`,
    repo lint-clean) — was previously stated as absent.
  - New paragraph on the Vite manual chunk split (`three` /
    `3d-force-graph` / `three-spritetext` → `three` chunk;
    `@codemirror` / `@uiw/react-codemirror` → `codemirror` chunk).
  - Added a CI line under Conventions pointing at
    `.github/workflows/ci.yml` (Node 24, `setup-node@v5`,
    `actions/checkout@v5`, lint + tests for both packages).

## Documentation pass (afternoon)

- [ARCHITECTURE.md] (new, repo root) Project-wide entry point for
  understanding how VortexFlow is built. 11 sections: overview, system
  diagram (Mermaid), request lifecycle, backend layers + routes table,
  frontend layers + provider tree + routing + 3D pipeline, data model
  (Mermaid ERD), auth & sessions, simulation pipeline (browser-side
  rationale + commit reference for the server-side removal), DOT 3D
  triple-invariant section with checklist for adding new attributes,
  cross-cutting concerns, pointers to topic-specific docs.
- [backend/doc/README.md] Rewrite. Removed: stale "Production Ready
  2025-07-07" framing, the public default admin credentials, and the
  links to the now-orphaned validation reports
  (FINAL_VALIDATION_REPORT, TEST_REPORT, REDIS_INTEGRATION_REPORT,
  TOOLKIT_SUMMARY, DOCUMENTATION_SUMMARY). Replaced with a sober index
  pointing at the 5 living topic guides (API, AUTH, CONFIG, DEPLOY,
  DEV), a stack/routes summary, a quickstart, and test/lint commands.
  Now defers to ARCHITECTURE.md at the repo root for the big picture.
## Code fixes (chantier 3.7 — hardcoded URL)

- [frontend/src/components/graphs/GraphRenderer3D.tsx] Replaced the
  hardcoded `http://192.168.5.30:5000/api/public/parse-dot` URL with
  the same env-var pattern that `services/api.ts` already uses:
  `import.meta.env.VITE_API_URL || 'http://localhost:5000/api'`. The
  app is now portable across dev / prod / LAN deployments without
  per-machine code edits. Surgical change — kept the surrounding
  fetch + fallback logic untouched. Removed the `console.log` that
  echoed the URL (it was helpful for debugging the hardcoded host,
  no longer relevant). The renderer's own test suite (7 tests) still
  passes since it mocks `globalThis.fetch`, not the URL.

## Notes
- `ruflo init` is per-project. Each new project needs it once to silence
  the hook-handler error; daemon / memory / swarm init are optional and
  also per-project.
- Setup chosen here: manual mode (no auto-init wrapper around `cd`, no
  systemd-user supervisor). The supervisor would be mono-project (the
  unit's `WorkingDirectory` is hard-coded), which doesn't fit a
  multi-project workflow.
- The 5 orphaned validation reports under `backend/doc/` (FINAL_VALIDATION,
  TEST_REPORT, REDIS_INTEGRATION, TOOLKIT_SUMMARY, DOCUMENTATION_SUMMARY)
  were *unlinked* from the index but **not deleted** — keep them for
  history or remove them in a separate cleanup pass.
